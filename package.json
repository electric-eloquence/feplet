{
  "name": "feplet",
  "version": "0.1.3",
  "description": "A Mustache-compatible templating engine. Powerful under the hood. Simple behind the wheel.",
  "main": "src/index.js",
  "scripts": {
    "bundle": "npx babel src -o dist/feplet.node.es5.js && node run/bundle-client.js",
    "devel": "npm run watch & npm run serve",
    "lint": "npx eslint run && npx eslint src && npx eslint test",
    "prepush": "npm run lint && npm test",
    "serve": "npx http-server .",
    "test": "npx mocha test/tests.js",
    "watch": "npx nodemon --watch src -e js --exec 'npm run bundle'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/electric-eloquence/feplet.git"
  },
  "keywords": [
    "fepper",
    "functional",
    "mustache",
    "template",
    "templating"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/electric-eloquence/feplet/issues"
  },
  "homepage": "https://github.com/electric-eloquence/feplet#readme",
  "dependencies": {
    "json-eval": "^1.0.1",
    "mkdirp": "0.5.1",
    "nopt": "3.0.6"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "browserify": "^14.4.0",
    "chai": "4.x",
    "eslint": "3.x",
    "http-server": "^0.11.1",
    "husky": "^0.14.3",
    "mocha": "5.x",
    "nodemon": "^1.14.12",
    "uglify-es": "3.3.9"
  },
  "bin": {
    "hulk": "bin/hulk"
  }
}
